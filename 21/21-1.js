var cities = [];
cities.push({ name : 'Rawalpindi', class: 'ASP.net 6.x' });
cities.push({ name : 'Islamabad', class: 'Metaverse' });
cities.push({ name : 'Lahore', class: 'AI' });

cities.forEach(m => console.log(`${m.class} course is being held in ${m.name}`));

